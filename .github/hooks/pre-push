#!/bin/bash

echo "🔄 Pull automatique avant push..."

# Pull avec rebase
git pull --rebase origin main

# Si le pull a échoué, arrêter le push
if [ $? -ne 0 ]; then
    echo "❌ Erreur lors du pull. Résolvez les conflits avant de push."
    exit 1
fi

echo "✅ Pull réussi, push en cours..."
exit 0